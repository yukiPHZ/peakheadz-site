# Case photo review

status: `publication_consent_pending`

Case photos remain unpublished until the company name, work description, and
photo permissions have all been confirmed. Source images and work-in-progress
edits are not stored in this repository.

After approval, recheck every source image. Remove or obscure client documents,
on-screen information, location data, and notifications, then export only
derived WebP images with EXIF removed to `public/assets/`.
